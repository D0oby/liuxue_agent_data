# AGENTS.md

`src/extract/` 只负责读取外部源文件并输出原始行 DTO。

## 目录职责

- 读取 Excel。
- 保留文件名、sheet 名、Excel 行号等来源信息。
- 跳过空行和缺少关键字段的无效行。

## 边界

- 不做课程推荐、招生要求标准化、RAG、评分或分档。
- 不写数据库。
- 不改变一行 Excel 等于一条课程记录的下游假设。
- 不按 `CRICOS` 去重。

## 验证

修改读取逻辑时，补充或更新导入相关测试，重点验证来源元信息和空行/缺字段处理。
