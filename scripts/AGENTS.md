# AGENTS.md

`scripts/` 保存辅助脚本，不是业务逻辑主入口。

## 目录职责

- 放置开发、发布、维护或一次性操作脚本。
- 脚本应调用 `src/` 中已有能力，而不是复制业务规则。

## 边界

- 不把推荐服务、RAG、评分或爬虫核心逻辑写在 shell 脚本里。
- 不写默认会删除、重置或覆盖数据的脚本。
- 涉及远程仓库、生产库或批量写入时，必须让用户明确确认。

## 验证

脚本变更至少要能通过 dry-run、帮助信息或局部命令验证；无法安全运行时说明原因。
